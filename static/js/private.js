// Private Mode Specific JavaScript

document.addEventListener('DOMContentLoaded', () => {
    console.log('Private mode JavaScript loaded');
    
    // Private mode specific functionality
    // Rating, classification, and category management are already handled in main.js
    // This file is for any additional private-mode-only features
    
    // Enable all interactive features for private mode
    // (Rating handlers are already set up in main.js via setupGalleryCards)
});
